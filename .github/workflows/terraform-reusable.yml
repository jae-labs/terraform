name: "Terraform Apply"

permissions:
  contents: read

on:
  workflow_call:
    inputs:
      working-directory:
        required: true
        type: string
      provider-token-name:
        required: true
        type: string
        description: "env var name for the provider token (e.g. GITHUB_TOKEN, DOPPLER_TOKEN)"
    secrets:
      GCP_SA_KEY:
        required: true
      PROVIDER_TOKEN:
        required: true

jobs:
  apply:
    runs-on: ubuntu-latest
    environment: production
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # ratchet:actions/checkout@v6

      - uses: hashicorp/setup-terraform@5e8dbf3c6d9deaf4193ca7a8fb23f2ac83bb6c85 # ratchet:hashicorp/setup-terraform@v4
        with:
          terraform_version: "~> 1.5"

      - name: GCP credentials
        run: echo '${{ secrets.GCP_SA_KEY }}' > /tmp/gcp-sa-key.json

      - name: Terraform init
        run: terraform init
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/gcp-sa-key.json

      - name: Terraform apply
        run: |
          export "${{ inputs.provider-token-name }}=${{ secrets.PROVIDER_TOKEN }}"
          terraform apply -auto-approve
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /tmp/gcp-sa-key.json

      - name: Cleanup credentials
        if: always()
        run: rm -f /tmp/gcp-sa-key.json
