# GCS backend auth
export GOOGLE_APPLICATION_CREDENTIALS=./gcp-sa-key.json

# GitHub: provider reads GITHUB_TOKEN automatically

# Doppler: provider reads DOPPLER_TOKEN automatically

# Cloudflare: provider reads CLOUDFLARE_API_TOKEN automatically
# scoped API token with Zone:Read/Edit, DNS:Read/Edit, Account:Read/Edit
